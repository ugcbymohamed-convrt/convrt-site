/* Shared Elyon art-direction tokens — dark luxury / champagne gold,
   sampled from the real Elyon wordmark. Kept local to this case study
   so the sitewide CONVRT palette (lime) stays the default everywhere else. */
export const ACCENT = '#c9a668'
export const ACCENT_LIGHT = '#e8d9b5'
export const ACCENT_DIM = '#8a7248'
